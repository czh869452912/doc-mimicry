# PRD Examples

Place best-practice PRD examples here.

Examples should teach structure, narrative style, information density, and organization. They should not be treated as semantically related source material unless the user explicitly says so.

Use the resource layout defined in `docs/architecture/workspace-contract.md`:

```text
examples/
  original/
  markdown/
  assets/
  reports/
```
